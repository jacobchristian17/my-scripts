from setuptools import setup, find_packages

setup(
    name="contact_info_checker",
    version="0.1.0",
    description="Detect contact information in text",
    py_modules=["contact_detector"],
    python_requires=">=3.7",
)
